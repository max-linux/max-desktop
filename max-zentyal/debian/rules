#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk


binary-install/max-zentyal::
	# clean svn dirs
	find $(CURDIR)/debian/max-zentyal -type d -name .svn | xargs rm -rf --

	rm -rf $(CURDIR)/debian/max-zentyal/etc/zentyal
