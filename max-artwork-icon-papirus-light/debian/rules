#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk
include /usr/share/cdbs/1/rules/aa-debhelper.mk

clean::
	make clean

binary-install/max-artwork-icon-papirus-light::
	make && make install
