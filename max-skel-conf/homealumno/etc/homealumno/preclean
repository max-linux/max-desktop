# comandos que se ejecutan antes de restaurar el skel

echo "homealumno::antes de restaurar.."

# cargar scripts de /etc/homealumno/preclean.d/

for f in /etc/homealumno/preclean.d/* ; do
  if [ -f "$f" ]; then
    . $f
  fi
done
