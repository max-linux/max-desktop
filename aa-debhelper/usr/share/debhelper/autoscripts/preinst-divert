if [ "$1" = install ]; then
	grep -q "^/etc" "#FILE#" 2>/dev/null && \
        ucf --purge "#FILE#"

	dpkg-divert --quiet --package #PACKAGE# --add --rename #FILE#

	if [ ! -e "#FILE#.divert" ] && \
           [ -e "#FILE#" ]; then
                cp "#FILE#" "#FILE#.divert"
        fi
fi
