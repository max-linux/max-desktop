# Defaults for max-bitten-slave initscript
# sourced by /etc/init.d/max-bitten-slave
# installed at /etc/default/max-bitten-slave by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="-f /etc/max-bitten-slave -i 30 -l /var/log/max-bitten-slave.log -d /var/lib/max-bitten-slave http://max.educa.madrid.org:8000/builds"
